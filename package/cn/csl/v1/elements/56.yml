# Tombstone. Duplicate of Article 56, already represented by `article_56`
# (externalId "Article 56"), which is the form every SCF crosswalk reference
# uses. This bare-number copy only ever held a generated placeholder description
# and is referenced by nothing. `deprecate: true` makes the dataloader delete the
# existing record; the file must stay for that deletion to be applied on load.
id: a3344676-0415-46c0-8f55-2b1f0f1aab66
externalId: "56"
name: "56"
description: Superseded by Article 56 (element `article_56`).
elementType: control
deprecate: true
