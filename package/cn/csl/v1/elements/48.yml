# Tombstone. Duplicate of Article 48, already represented by `article_48`
# (externalId "Article 48"), which is the form every SCF crosswalk reference
# uses. This bare-number copy only ever held a generated placeholder description
# and is referenced by nothing. `deprecate: true` makes the dataloader delete the
# existing record; the file must stay for that deletion to be applied on load.
id: 18e5a3b6-7300-4313-a659-99e5a1fe3238
externalId: "48"
name: "48"
description: Superseded by Article 48 (element `article_48`).
elementType: control
deprecate: true
