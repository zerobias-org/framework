# Tombstone. Duplicate of Article 21, already represented by `article_21`
# (externalId "Article 21"), which is the form every SCF crosswalk reference
# uses. This bare-number copy only ever held a generated placeholder description
# and is referenced by nothing. `deprecate: true` makes the dataloader delete the
# existing record; the file must stay for that deletion to be applied on load.
id: f6dc7148-92f7-49fb-880d-49bf732afd0c
externalId: "21"
name: "21"
description: Superseded by Article 21 (element `article_21`).
elementType: control
deprecate: true
