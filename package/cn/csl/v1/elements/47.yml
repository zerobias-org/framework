# Tombstone. Duplicate of Article 47, already represented by `article_47`
# (externalId "Article 47"), which is the form every SCF crosswalk reference
# uses. This bare-number copy only ever held a generated placeholder description
# and is referenced by nothing. `deprecate: true` makes the dataloader delete the
# existing record; the file must stay for that deletion to be applied on load.
id: e63f18ad-0bc6-469a-ab11-bd1fb63deba6
externalId: "47"
name: "47"
description: Superseded by Article 47 (element `article_47`).
elementType: control
deprecate: true
