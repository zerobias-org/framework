# Tombstone. Duplicate of Article 42, already represented by `article_42`
# (externalId "Article 42"), which is the form every SCF crosswalk reference
# uses. This bare-number copy only ever held a generated placeholder description
# and is referenced by nothing. `deprecate: true` makes the dataloader delete the
# existing record; the file must stay for that deletion to be applied on load.
id: 2098576b-5986-434e-a5ee-3ab9f3eef15b
externalId: "42"
name: "42"
description: Superseded by Article 42 (element `article_42`).
elementType: control
deprecate: true
