# Tombstone. IR-4(16) has no counterpart in any published SP 800-53 release —
# not in Rev 5.1.1, not in NIST's OSCAL catalog — and this copy only ever held a
# generated placeholder name and description. Referenced by no crosswalk.
# `deprecate: true` makes the dataloader delete the record it loaded from earlier
# versions of this package; the file must stay for that deletion to be applied.
id: 8b970738-15e6-4ef9-b4e6-e32d6903e079
externalId: IR-4(16)
name: IR-4(16)
description: Removed — no such control in any SP 800-53 release.
elementType: control
deprecate: true
